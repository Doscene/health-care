# 完善 Phase 1-3 中未完成或骨架占用的功能

## 背景

经代码审查，Phase 1-3 的大部分功能已实现，但存在以下明确的 TODO 骨架和缺失功能：

---

## 一、确认已完成（不需要改动）

- 服务端定时任务（B2-16/B2-17）：`medication-reminder.scheduler.ts`、`risk-scan.scheduler.ts` 已实现
- Android WorkManager（A2-15）：`MedicationReminderWorker.kt` 已实现
- Phase 3 后端 API：家庭总结、化学力、目标、挑战、提醒、饮食推荐、购物清单全部已实现
- Android 基础屏幕：登录、角色选择、家庭圈、记录、用药、饮食、急救包、紧急联系人等全部已实现

---

## 二、需要完善的任务

### 任务 1：Android 端 —— 完善隐私设置页 TODO 开关
**文件**: `android/app/src/main/java/com/healthcare/family/ui/profile/PrivacyScreen.kt`
- L83: "接收用药提醒推送" 开关 → 用 `TokenManager` 或本地 SharedPreferences 存储用户偏好
- L92: "接收风险告警推送" 开关 → 同上
- 实现：增加 `TokenManager` 中的 `pushMedicationReminder` 和 `pushRiskAlert` 属性，切换时持久化

### 任务 2：Android 端 —— 复制邀请码 + 分享功能
**文件**: `android/app/src/main/java/com/healthcare/family/ui/family/CreateFamilyScreen.kt`
- L162: 复制邀请码按钮 → 使用 `ClipboardManager` 复制（参考 `FamilyScreen.kt` 中已有的 `copyToClipboard` 实现）
- L167: 分享按钮 → 使用 `Intent.ACTION_SEND` 系统分享

### 任务 3：Android 端 —— 补充 HealthCareApi 缺失的 Phase 3 端点
**文件**: `android/app/src/main/java/com/healthcare/family/data/remote/api/HealthCareApi.kt`
- 家庭协作 API：
  - `GET family/{familyId}/summary` → 家庭健康总结
  - `GET family/{familyId}/chemistry` → 化学力/协同分数
  - `POST/GET family/{familyId}/goals` → 家庭目标 CRUD
  - `POST/GET family/{familyId}/challenges` → 家庭挑战
  - `POST/GET family/{familyId}/reminders` → 互相提醒
  - `POST family/{familyId}/reminders/{reminderId}/complete` → 完成提醒
- 饮食管理 API：
  - `POST diet/record` → 饮食记录
  - `GET diet/record` → 饮食记录列表
  - `GET/POST diet/menu/custom` → 自定义菜单
  - `GET diet/shopping-list` → 购物清单
  - `POST diet/shopping-list/save` → 保存购物清单
  - `PUT diet/shopping-list/{listId}/item/{itemIndex}/check` → 勾选购物项
- 对应的 DTO 类（FamilyGoalDto, FamilyChallengeDto, FamilyReminderDto, ChemistryDto, DietRecordDto, ShoppingListDto 等）

### 任务 4：Android 端 —— FamilyRepository 补充 Phase 3 方法
**文件**: `android/app/src/main/java/com/healthcare/family/data/repository/FamilyRepository.kt`
- 添加：getFamilySummary, getChemistry, getGoals, createGoal, updateGoal, deleteGoal, getChallenges, createChallenge, joinChallenge, getReminders, createReminder, completeReminder

### 任务 5：Android 端 —— DietRepository 补充缺失方法
**文件**: `android/app/src/main/java/com/healthcare/family/data/repository/DietRepository.kt`
- 添加：getRecipes（已有），getCustomMenu, saveCustomMenu, recordDiet, getDietRecords, getShoppingList, saveShoppingList, checkShoppingItem

### 任务 6：Android 端 —— FamilyScreen 补充 Phase 3 协作功能卡片
**文件**: `android/app/src/main/java/com/healthcare/family/ui/family/FamilyScreen.kt`
在成员列表下方添加：
- 化学力/协同分数卡片（显示家庭化学力分数和完成率）
- 家庭目标卡片（显示当前目标 + 进度条）
- 家庭挑战卡片（显示进行中的挑战 + 参与按钮）
- 互相提醒入口
- 点击成员跳转成员详情页

### 任务 7：Android 端 —— FamilyViewModel 补充 Phase 3 数据加载
**文件**: `android/app/src/main/java/com/healthcare/family/ui/family/FamilyViewModel.kt`
- 扩展 UiState：增加 chemistryScore, goals, challenges, reminders 字段
- 扩展 loadFamily：加载家庭总结、化学力、目标、挑战数据

### 任务 8：Android 端 —— MenuScreen 补充购物清单功能
**文件**: `android/app/src/main/java/com/healthcare/family/ui/diet/MenuScreen.kt`
- 在今日菜单底部添加 "生成购物清单" 按钮
- 点击后调用 `GET diet/shopping-list` 生成购物清单
- 展示购物清单列表，支持勾选

### 任务 9：Android 端 —— DietViewModel/DietScreen 补充饮食记录功能
**文件**: `android/app/src/main/java/com/healthcare/family/ui/diet/DietViewModel.kt` + `DietScreen.kt`
- DietScreen 添加 "记录饮食" 快捷入口
- DietViewModel 添加饮食记录相关状态和方法

### 任务 10：服务端 —— 补充隐私设置更新 API
**文件**: 
- `server/src/modules/family/family.controller.ts` — 添加 `PUT family/{familyId}/members/{memberId}/visibility` 端点
- `server/src/modules/family/family.service.ts` — 添加 `updateMemberVisibility` 方法
- 更新 `VisibilityConfig` 的默认值以支持用户持久化

### 任务 11：Android 端 —— NavGraph 补充缺失路由
**文件**: `android/app/src/main/java/com/healthcare/family/navigation/NavGraph.kt`
- 添加 `diet/record`（饮食记录页）
- 添加 `diet/shopping`（购物清单页）
- 添加 `family/{familyId}/member/{memberId}`（成员健康详情页）
- 添加 `family/goals`（家庭目标页）
- 添加 `family/challenges`（家庭挑战页）
- 添加 `family/reminders`（互相提醒页）

### 任务 12：Android 端 —— 新增 Phase 3 子页面
需要新增以下 Composable 页面：
- `MemberDetailScreen.kt` — 成员健康详情（血压/血糖/用药 tabs）
- `FamilyGoalScreen.kt` — 家庭目标管理
- `FamilyChallengeScreen.kt` — 家庭挑战列表
- `FamilyReminderScreen.kt` — 互相提醒列表 + 发送
- `ShoppingListScreen.kt` — 购物清单
- `DietRecordScreen.kt` — 饮食拍照/手动记录

---

## 三、执行顺序（仅完善已有骨架的 TODO，不包含第三方 SDK 集成如微信登录、Room 缓存等）

1. **Android UI 骨架修复**：任务 1 + 2（PrivacyScreen TODO 开关、CreateFamilyScreen 复制/分享）→ 简单快速
2. **服务端**：任务 10（隐私设置更新 API）→ 补充 visibility 配置端点
3. **Android API 层**：任务 3 + 5 + 4（API 定义 + Repository 补充 Phase 3 端点）→ 为后续 UI 打基础
4. **Android Phase 3 UI**：任务 6 + 7 + 8 + 9 + 11 + 12（新页面 + 路由 + 数据加载）

---

## 四、验证

- `cd server && npm run lint` — 服务端代码无 lint 错误
- `cd server && npm run build` — 服务端编译成功
- `cd android && ./gradlew assembleDebug` — Android 编译成功
- 手动验证：隐私设置开关切换后数据持久化；邀请码可复制/分享；家庭圈显示化学力/目标/挑战卡片
